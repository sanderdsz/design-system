{
  "name": "@sanderdsz/packages",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "license": "ISC",
  "devDependencies": {
    "turbo": "^1.5.6"
  }
}
